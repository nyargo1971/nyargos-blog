---
title: "０．１（レイ・テン・イチ）から始める、ＡＩ（ＬＬＭ）を使ったプログラム生成【６－１】"
date: 04/10/2026 22:59:48
category: "パソコン・インターネット"
---

<p>●main.pyのソースコード</p>
<p>※実際のＰｙｔｈｏｎコードでは”厳密な”インデント管理が必要です。ブログへの貼り付けのため反映されていません。</p>
<p>　</p>
<p># main.py</p>
<p>from core.model_environment import Environment<br />from core.model_NN005 import NN005Model<br />from core.model_NN007 import NN007Model<br />from core.model_mentor import Mentor</p>
<p>from state.state_environment import EnvironmentState<br />from state.state_nn005 import NN005State<br />from state.state_nn007 import NN007State</p>
<p>from gui.gui_main import GUIMain<br />from log.log_environment import EnvironmentLogger<br />from log.log_nn005 import NN005Logger<br />from log.log_nn007 import NN007Logger<br />from config.config import MAX_TURN</p>
<p><br />def ask_float(prompt: str) -&gt; float:<br />while True:<br />try:<br />return float(input(prompt))<br />except ValueError:<br />print("数値を入力してください。")</p>
<p><br />def ask_yes_no(prompt: str) -&gt; bool:<br />while True:<br />s = input(prompt).strip().lower()<br />if s == "y":<br />return True<br />if s == "n":<br />return False<br />print("y または n を入力してください。")</p>
<p><br />def game_once() -&gt; None:<br />print("=== NN007：交点発見ゲーム ===")<br />print("x軸との交点をニューラルネットに推定させるゲームです。")<br />print("これから、秘密の真円 X の中心と半径を設定してください。")<br />print()</p>
<p>while True:<br />x = ask_float("真円の中心 x を入力してください（-99～99）：")<br />y = ask_float("真円の中心 y を入力してください（-99～99）：")<br />r = ask_float("真円の半径 r を入力してください（10～99）：")</p>
<p>env_state = EnvironmentState()<br />env = Environment(env_state)<br />try:<br />env.initialize_with_input(x, y, r)<br />except ValueError as e:<br />print("環境エラー：", e)<br />print("もう一度入力してください。")<br />continue<br />break</p>
<p>input("ゲームを開始します。Enter を押してください...")</p>
<p>nn005_state = NN005State()<br />nn007_state = NN007State()<br />nn005_model = NN005Model()<br />nn007_model = NN007Model()<br />mentor = Mentor()<br />gui = GUIMain()</p>
<p>log_env = EnvironmentLogger()<br />log_005 = NN005Logger()<br />log_007 = NN007Logger()</p>
<p>epilogue_case = None</p>
<p>for turn in range(1, MAX_TURN + 1):<br />hint1, hint2 = env.generate_hints()<br />log_env.write(turn, hint1, hint2)</p>
<p>hint_mid = (hint1 + hint2) / 2.0<br />x_norm_mid = env.normalize(hint_mid)<br />s005_pred = nn005_model.predict(x_norm_mid, nn005_state)<br />s005_correct = env.judge(hint_mid)<br />nn005_model.learn(x_norm_mid, s005_correct, nn005_state)</p>
<p>mentor_msg_dummy = "" # placeholder</p>
<p>if nn005_state.mu is not None:<br />from config.config import HINT_MIN, HINT_MAX<br />nn005_state.mu_real = HINT_MIN + nn005_state.mu * (HINT_MAX - HINT_MIN)</p>
<p># [E:#x2605] ここで「影の重み」にコピーする<br />nn005_state.mu_alt = nn005_state.mu<br />nn005_state.mu_alt_real = nn005_state.mu_real</p>
<p>log_005.write(<br />turn,<br />hint_mid,<br />s005_pred,<br />s005_correct,<br />nn005_state.mu,<br />nn005_state.mu_real,<br />)</p>
<p>x1_norm = env.normalize(hint1)<br />x2_norm = env.normalize(hint2)<br />s1_pred, s2_pred = nn007_model.predict(x1_norm, x2_norm, nn007_state)<br />s1_correct = env.judge(hint1)<br />s2_correct = env.judge(hint2)<br />nn007_model.learn(x1_norm, x2_norm, s1_correct, s2_correct, nn007_state)</p>
<p>from config.config import HINT_MIN, HINT_MAX<br />if nn007_state.x1_norm is not None:<br />nn007_state.x1_real = HINT_MIN + nn007_state.x1_norm * (HINT_MAX - HINT_MIN)<br />if nn007_state.x2_norm is not None:<br />nn007_state.x2_real = HINT_MIN + nn007_state.x2_norm * (HINT_MAX - HINT_MIN)</p>
<p>log_007.write(<br />turn,<br />hint1,<br />hint2,<br />s1_pred,<br />s2_pred,<br />s1_correct,<br />s2_correct,<br />nn007_state.x1_norm or 0.0,<br />nn007_state.x2_norm or 0.0,<br />nn007_state.x1_real or 0.0,<br />nn007_state.x2_real or 0.0,<br />)</p>
<p>mentor_message = mentor.update_after_turn(<br />env_state,<br />nn005_state,<br />nn007_state,<br />hint1,<br />hint2,<br />)</p>
<p># --- 収束判定（モデル別に epilogue を出す） ---<br />if mentor.converged_flag:<br />epilogue_case = "converged"</p>
<p># まず両方を空にする<br />gui.epilogue_text_left = ""<br />gui.epilogue_text_right = ""</p>
<p># 1層モデルが収束した場合<br />if mentor.converged_model == "NN005":<br />gui.epilogue_text_left = "メンター収束判定！"</p>
<p># 2層モデルが収束した場合<br />if mentor.converged_model == "NN007":<br />gui.epilogue_text_right = "メンター収束判定！"</p>
<p>gui.update(env_state, nn005_state, nn007_state, mentor_message)<br />break</p>
<p># --- MAXターン到達 ---<br />if turn &gt;= MAX_TURN:<br />epilogue_case = "max"<br />gui.epilogue_text_left = "MAXターン到達"<br />gui.epilogue_text_right = "MAXターン到達"<br />gui.update(env_state, nn005_state, nn007_state, mentor_message)<br />break</p>
<p>if not gui.update(env_state, nn005_state, nn007_state, mentor_message):<br />return</p>
<p>if epilogue_case is not None:<br />while True:<br />if not gui.update(env_state, nn005_state, nn007_state, mentor_message):<br />break</p>
<p>print("～エピローグ～")</p>
<p><br />def main():<br />while True:<br />game_once()<br />if not ask_yes_no("～エピローグ～ 再ゲームしますか？(y/n)："):<br />break<br />print("終了します。")</p>
<p><br />if __name__ == "__main__":<br />main()</p>
<p>　 </p>
<p> 　</p>