---
title: "０．１（レイ・テン・イチ）から始める、ＡＩ（ＬＬＭ）を使ったプログラム生成【６－１３】"
date: 04/10/2026 23:07:15
category: "パソコン・インターネット"
---

<p>●gui_main.pyのソースコード</p>
<p>※実際のＰｙｔｈｏｎコードでは”厳密な”インデント管理が必要です。ブログへの貼り付けのため反映されていません。</p>
<p>※pygameをインポートしているため、pygameのインスト―ルが、別途必要な場合があります。</p>
<p>　</p>
<p># gui/gui_main.py</p>
<p>import pygame</p>
<p>from state.state_environment import EnvironmentState<br />from state.state_nn005 import NN005State<br />from state.state_nn007 import NN007State</p>
<p><br />class GUIMain:<br />def __init__(self, width: int = 900, height: int = 700):<br />pygame.init()<br />self.screen = pygame.display.set_mode((width, height))<br />pygame.display.set_caption("NN007/円のx軸交点探索ゲーム")<br />self.clock = pygame.time.Clock()<br />self.font = pygame.font.SysFont("msgothic", 20)<br />self.running = True</p>
<p>self.width = width<br />self.height = height</p>
<p>self.panel_size = 300<br />self.panel_margin_top = 120<br />self.panel_margin_side = 80</p>
<p>self.close_button_rect = pygame.Rect(<br />width // 2 - 60, height - 80, 120, 40<br />)</p>
<p>self.epilogue_text_left = ""<br />self.epilogue_text_right = ""</p>
<p>def _draw_text_center(self, text: str, y: int, color=(0, 0, 0)):<br />surf = self.font.render(text, True, color)<br />rect = surf.get_rect(center=(self.width // 2, y))<br />self.screen.blit(surf, rect)</p>
<p>def _draw_text(self, text: str, x: int, y: int, color=(0, 0, 0)):<br />surf = self.font.render(text, True, color)<br />self.screen.blit(surf, (x, y))</p>
<p>def _panel_origin(self, left_panel: bool) -&gt; tuple[int, int]:<br />if left_panel:<br />x0 = self.panel_margin_side<br />else:<br />x0 = self.width - self.panel_margin_side - self.panel_size<br />y0 = self.panel_margin_top<br />return x0, y0</p>
<p>def _world_to_panel(<br />self, x: float, y: float, left_panel: bool<br />) -&gt; tuple[int, int]:<br />x0, y0 = self._panel_origin(left_panel)<br />scale = self.panel_size / 200 # -100〜100 を 300px に収める<br />sx = x0 + int(self.panel_size / 2 + x * scale)<br />sy = y0 + int(self.panel_size / 2 - y * scale)<br />return sx, sy</p>
<p>def _draw_axes(self, left_panel: bool):<br />x0, y0 = self._panel_origin(left_panel)<br />rect = pygame.Rect(x0, y0, self.panel_size, self.panel_size)<br />pygame.draw.rect(self.screen, (0, 0, 0), rect)<br />cx = x0 + self.panel_size // 2<br />cy = y0 + self.panel_size // 2<br />pygame.draw.line(self.screen, (200, 200, 200), (x0, cy), (x0 + self.panel_size, cy), 1)<br />pygame.draw.line(self.screen, (200, 200, 200), (cx, y0), (cx, y0 + self.panel_size), 1)</p>
<p>for val in [-50, 0, 50]:<br />sx, sy = self._world_to_panel(val, 0, left_panel)<br />self._draw_text(str(val), sx - 10, cy + 5, (200, 200, 200))<br />for val in [-50, 0, 50]:<br />sx, sy = self._world_to_panel(0, val, left_panel)<br />self._draw_text(str(val), cx + 5, sy - 10, (200, 200, 200))</p>
<p>def update(<br />self,<br />env_state: EnvironmentState,<br />nn005_state: NN005State,<br />nn007_state: NN007State,<br />mentor_message: str,<br />) -&gt; bool:<br />for event in pygame.event.get():<br />if event.type == pygame.QUIT:<br />self.running = False<br />elif event.type == pygame.MOUSEBUTTONDOWN and event.button == 1:<br />if self.close_button_rect.collidepoint(event.pos):<br />self.running = False</p>
<p>if not self.running:<br />pygame.quit()<br />return False</p>
<p>self.screen.fill((220, 220, 220))</p>
<p>self._draw_text_center("NN007/円のx軸交点探索ゲームver7.3.4", 30)<br />self._draw_text_center("秘密の真円Xと座標上のx軸との交点を、各モデルが探索します", 60)<br />self._draw_text_center("１層モデル　　　　　　　　　　　　　　　　２層モデル", 110)</p>
<p>self._draw_axes(left_panel=True)<br />self._draw_axes(left_panel=False)</p>
<p># 秘密の円（両パネルに同じもの）<br />if (<br />env_state.target_x is not None<br />and env_state.target_y is not None<br />and env_state.target_r is not None<br />):<br />for left in [True, False]:<br />cx, cy = self._world_to_panel(env_state.target_x, env_state.target_y, left)<br />scale = self.panel_size / 200<br />r = int(env_state.target_r * scale)<br />pygame.draw.circle(self.screen, (255, 0, 0), (cx, cy), r, 2)</p>
<p># NN005: 過去ターン（青）＋当該ターン（黄）<br />for x_real in nn005_state.history_mu_real:<br />sx, sy = self._world_to_panel(x_real, 0.0, True)<br />pygame.draw.circle(self.screen, (0, 0, 255), (sx, sy), 2)<br />if nn005_state.mu_real is not None:<br />sx, sy = self._world_to_panel(nn005_state.mu_real, 0.0, True)<br />pygame.draw.circle(self.screen, (255, 255, 0), (sx, sy), 3)</p>
<p># NN007: 過去ターン（青）＋当該ターン（黄）<br />for x_real in nn007_state.history_x1_real:<br />sx, sy = self._world_to_panel(x_real, 0.0, False)<br />pygame.draw.circle(self.screen, (0, 0, 255), (sx, sy), 2)<br />for x_real in nn007_state.history_x2_real:<br />sx, sy = self._world_to_panel(x_real, 0.0, False)<br />pygame.draw.circle(self.screen, (0, 0, 255), (sx, sy), 2)<br />if nn007_state.x1_real is not None:<br />sx, sy = self._world_to_panel(nn007_state.x1_real, 0.0, False)<br />pygame.draw.circle(self.screen, (255, 255, 0), (sx, sy), 3)<br />if nn007_state.x2_real is not None:<br />sx, sy = self._world_to_panel(nn007_state.x2_real, 0.0, False)<br />pygame.draw.circle(self.screen, (255, 255, 0), (sx, sy), 3)</p>
<p># --- 05-1：エピローグ（収束判定 / MAXターン） ---<br />epilogue_y = self.panel_margin_top + self.panel_size + 10</p>
<p># 左パネル側<br />if self.epilogue_text_left:<br />self._draw_text(<br />self.epilogue_text_left,<br />self.panel_margin_side,<br />epilogue_y,<br />(0, 0, 0)<br />)</p>
<p># 右パネル側<br />if self.epilogue_text_right:<br />self._draw_text(<br />self.epilogue_text_right,<br />self.width - self.panel_margin_side - self.panel_size,<br />epilogue_y,<br />(0, 0, 0)<br />)</p>
<p># --- 05-2：mentor_message（進行メッセージ） ---<br />mentor_msg_y = epilogue_y + 50 # 05-1 の下に配置<br />if mentor_message:<br />self._draw_text_center(<br />mentor_message,<br />mentor_msg_y,<br />(0, 0, 0)<br />)</p>
<p># CLOSE 誘導<br />self._draw_text_center("ウインドウを閉じる場合は下のボタンを押してください", self.height - 110)</p>
<p># --- CLOSE ボタン（Windows 風エンボス） ---<br />mx, my = pygame.mouse.get_pos()<br />hover = self.close_button_rect.collidepoint((mx, my))</p>
<p># ボタン背景色（ホバーで少し濃く）<br />base_color = (200, 200, 200) if not hover else (170, 170, 170)<br />pygame.draw.rect(self.screen, base_color, self.close_button_rect)</p>
<p># エンボス枠（上・左：白 / 下・右：黒）<br />x, y, w, h = self.close_button_rect<br />pygame.draw.line(self.screen, (255, 255, 255), (x, y), (x + w, y), 2) # 上<br />pygame.draw.line(self.screen, (255, 255, 255), (x, y), (x, y + h), 2) # 左<br />pygame.draw.line(self.screen, (0, 0, 0), (x, y + h), (x + w, y + h), 2) # 下<br />pygame.draw.line(self.screen, (0, 0, 0), (x + w, y), (x + w, y + h), 2) # 右</p>
<p># ボタンテキスト（中央）<br />self._draw_text_center("CLOSE", self.close_button_rect.centery)</p>
<p>pygame.display.flip()<br />self.clock.tick(30)<br />return True</p>
<p>　 </p>
<p> 　</p>