---
title: "０．１（レイ・テン・イチ）から始める、ＡＩ（ＬＬＭ）を使ったプログラム生成【６－２】"
date: 04/10/2026 23:03:13
category: "パソコン・インターネット"
---

<p>●model_mentor.pyのソースコード</p>
<p>※実際のＰｙｔｈｏｎコードでは”厳密な”インデント管理が必要です。ブログへの貼り付けのため反映されていません。</p>
<p>　</p>
<p># core/model_mentor.py</p>
<p>import sqlite3<br />from typing import Optional</p>
<p>from config.config import (<br />HINT_MIN, HINT_MAX,<br />MENTOR_CONVERGE_TURN, MENTOR_CONVERGE_EPS,<br />MENTOR_SKIP_CONVERGE_TURN,<br />)<br />from state.state_environment import EnvironmentState<br />from state.state_nn005 import NN005State<br />from state.state_nn007 import NN007State</p>
<p><br />class Mentor:<br />def __init__(self, db_path: str = "nn007.sqlite3"):<br />self.turn_count = 0<br />self.converged_flag = False<br />self.converged_model = None<br />self.db_path = db_path<br />self._init_db()</p>
<p>def _init_db(self) -&gt; None:<br />conn = sqlite3.connect(self.db_path)<br />cur = conn.cursor()<br />cur.execute(<br />"""<br />CREATE TABLE IF NOT EXISTS turns (<br />id INTEGER PRIMARY KEY AUTOINCREMENT,<br />turn INTEGER,<br />hint1 REAL,<br />hint2 REAL,<br />mu_real REAL,<br />x1_real REAL,<br />x2_real REAL,<br />err_left REAL,<br />err_right REAL<br />)<br />"""<br />)<br />conn.commit()<br />conn.close()</p>
<p>def _to_real(self, x_norm: float) -&gt; float:<br />return HINT_MIN + x_norm * (HINT_MAX - HINT_MIN)</p>
<p>def update_after_turn(<br />self,<br />env_state: EnvironmentState,<br />nn005_state: NN005State,<br />nn007_state: NN007State,<br />hint1: float,<br />hint2: float,<br />) -&gt; str:<br />self.turn_count += 1</p>
<p># --- 実数座標への変換と履歴保存 ---<br />if nn005_state.mu is not None:<br />nn005_state.mu_real = self._to_real(nn005_state.mu)<br />nn005_state.history_mu_real.append(nn005_state.mu_real)</p>
<p>if nn005_state.mu_alt is not None:<br />nn005_state.mu_alt_real = self._to_real(nn005_state.mu_alt)</p>
<p>if nn007_state.x1_norm is not None:<br />nn007_state.x1_real = self._to_real(nn007_state.x1_norm)<br />nn007_state.history_x1_real.append(nn007_state.x1_real)<br />if nn007_state.x2_norm is not None:<br />nn007_state.x2_real = self._to_real(nn007_state.x2_norm)<br />nn007_state.history_x2_real.append(nn007_state.x2_real)</p>
<p>err_left: Optional[float] = None<br />err_right: Optional[float] = None<br /><br /># --- 収束判定スキップ ---<br />if self.turn_count &lt;= MENTOR_SKIP_CONVERGE_TURN:<br />return f"ターン {self.turn_count}: 学習中..."</p>
<p># ============================================================<br /># [E:#x2605] 統一収束判定ロジック<br /># - Mentor は「交点か接点か」を考えない<br /># - ただ淡々と「2点の誤差」を見る<br /># - まず NN007 をチェック、ダメなら NN005 をチェック<br /># ============================================================<br />if (<br />env_state.target_left is not None<br />and env_state.target_right is not None<br />):<br /># --- まず NN007 の 2点推定を評価 ---<br />if (<br />nn007_state.x1_real is not None<br />and nn007_state.x2_real is not None<br />):<br />err_left = abs(env_state.target_left - nn007_state.x1_real)<br />err_right = abs(env_state.target_right - nn007_state.x2_real)</p>
<p>if err_left &lt; MENTOR_CONVERGE_EPS and err_right &lt; MENTOR_CONVERGE_EPS:<br />self.converged_flag = True<br />self.converged_model = "NN007"</p>
<p># --- NN007 がまだ収束していない場合、NN005 を評価 ---<br />if not self.converged_flag and nn005_state.mu_real is not None and nn005_state.mu_alt_real is not None:<br /># NN005 は 1 本しか出さないが、管理系が μ と μ_alt に複製している<br /># Mentor はそれを「2点の推定値」として淡々と扱う<br />err_left = abs(env_state.target_left - nn005_state.mu_real)<br />err_right = abs(env_state.target_right - nn005_state.mu_alt_real)</p>
<p>if err_left &lt; MENTOR_CONVERGE_EPS and err_right &lt; MENTOR_CONVERGE_EPS:<br />self.converged_flag = True<br />self.converged_model = "NN005"</p>
<p># --- ログ保存 ---<br />conn = sqlite3.connect(self.db_path)<br />cur = conn.cursor()<br />cur.execute(<br />"""<br />INSERT INTO turns (turn, hint1, hint2, mu_real, x1_real, x2_real, err_left, err_right)<br />VALUES (?, ?, ?, ?, ?, ?, ?, ?)<br />""",<br />(<br />self.turn_count,<br />hint1,<br />hint2,<br />nn005_state.mu_real,<br />nn007_state.x1_real,<br />nn007_state.x2_real,<br />err_left,<br />err_right,<br />),<br />)<br />conn.commit()<br />conn.close()</p>
<p># --- メッセージ生成 ---<br />if self.converged_flag:</p>
<p># どのモデルが勝ったかで推定値を切り替える<br />if self.converged_model == "NN007":<br />left_est = nn007_state.x1_real<br />right_est = nn007_state.x2_real<br />else: # NN005<br />left_est = nn005_state.mu_real<br />right_est = nn005_state.mu_alt_real</p>
<p>return f"最終リザルト！（ターン：{self.turn_count} / 左推定値：{left_est:.1f} / 右推定値：{right_est:.1f}）"<br /><br />if self.turn_count &gt;= MENTOR_CONVERGE_TURN:<br />return f"学習中…（まだ収束していません）ターン: {self.turn_count} "</p>
<p>return f"ターン {self.turn_count}: 学習中..."</p>
<p>　 </p>
<p> 　</p>